# Career Navigator Extension
# Note: API keys are configured by users directly in the extension settings
# No environment variables are required for the extension to work

# The .env file is no longer needed for production builds
# Users configure their own OpenAI API key via the extension's options page
